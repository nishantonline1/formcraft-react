/// <reference lib="dom" />

// Global type declarations for tests and development
declare global {
    // DOM types are available globally
    // eslint-disable-next-line @typescript-eslint/no-empty-object-type
    interface Window {
        // Add any custom window properties here if needed
    }
}

export { };